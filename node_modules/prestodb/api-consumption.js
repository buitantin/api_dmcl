'use strict';

const PrestoClient = require('./lib');

let prestoClient = new PrestoClient({
  url: 'http://profiler-reader-01.reddrummer.com:8889',
  user: 'presto'
});

prestoClient.sendStatement('SELECT * FROM hive.vivo.linhas_tmp_201703 LIMIT 10000')
  .then(result => console.log(result))
  .catch(error => console.error({ error }));
